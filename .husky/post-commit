#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Auto-push on every commit was removed for security: it published work
# (potentially including secrets or unreviewed changes) to the remote before
# any review. Push explicitly with `git push` when you are ready.
exit 0
